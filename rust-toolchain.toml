[toolchain]
# Pinned for reproducible builds (ARCHITECTURE.md §24.12).
# Bump deliberately, in its own commit, with CI green.
channel = "stable"
components = ["rustfmt", "clippy"]
targets = ["x86_64-pc-windows-msvc"]
profile = "default"
